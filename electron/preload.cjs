// preload — reserved. The atelier UI talks to the local server over http/ws
// (same as in a browser), so no privileged IPC bridge is needed yet. Kept so
// contextIsolation has a preload to attach and future native hooks have a home.
